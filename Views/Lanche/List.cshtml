@model IEnumerable<LanchesJa.Models.Lanche>

	<div>
		<h2>Todos os lanches</h2>
	</div>

	@foreach(var lanche in Model)
	{
		<h4>@lanche.Nome</h4>
		<p></p>
		<p><img src="@lanche.ImagemUrl"/></p>
		<h3>Preço: @lanche.Preco.ToString("c")</h3>

	}